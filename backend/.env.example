# === Database (Supabase Postgres, via Prisma) ===
# Supabase Dashboard -> Project Settings -> Database
# Transaction pooler (port 6543) -- used by the app at runtime
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
# Direct connection (port 5432) -- used only by `prisma migrate`
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# === Supabase Auth ===
# Supabase Dashboard -> Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL="https://[project-ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
# Server-only. Never expose to the frontend or commit a real value.
SUPABASE_SERVICE_ROLE_KEY=""

# 도메인 검사는 하지 않는다 — 학회원 계정이 ghsnu.com/gmail.com/snu.ac.kr 등
# 여러 도메인을 섞어 쓰므로, 접근 통제는 members 테이블 화이트리스트가 전담한다
# (npm run members:add). Google OAuth consent screen은 External로 설정한다.

# === OpenAI ===
OPENAI_API_KEY="sk-..."

# === Inngest ===
# 로컬 개발(inngest-cli dev)에서는 비워둬도 됨. 배포 시 Inngest 대시보드에서 발급.
INNGEST_EVENT_KEY=""
INNGEST_SIGNING_KEY=""
